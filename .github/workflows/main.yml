on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.0.0
      with:
        server: richard-dobbs.dreamhost.com
        username: dh_8hmfnw
        password: ${{ secrets.ftp_password }}
        protocol: ftps
        port: 22
        timeout: 60000
        server-dir: ./api.vsmsystem.com/
